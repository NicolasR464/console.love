//  import { test, expect } from "@playwright/test";
// ; 
// test('7DeleteUserAdmin', async ({ page }) => {
//     await page.goto('http://localhost:3000/admin');


// await page.getByRole('cell', { name: '' }).click()

// await page.waitForTimeout(500);


//   page.once('dialog', dialog => {
//     console.log(`Dialog message: ${dialog.message()}`);
//     dialog.dismiss().catch(() => {});
//   });
//   await page.getByRole('row', { name: 'Merge  Matthiew Perry 2000-01-01 Matthieu le giga bg no data no data C++: C#: Male Male Female Other No Edit Delete' }).getByRole('button', { name: 'Delete' }).click();

// // a verifier

//   //await page.waitForTimeout(500);
// await 


// });
